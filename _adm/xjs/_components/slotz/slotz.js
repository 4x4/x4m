var _slotz = Class.create(
{
    
    initialize: function (options)
        {
            this.options = {}
            Object.extend(this.options,options || {});
        }
        
        
        
    
});